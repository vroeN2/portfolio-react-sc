import styled from "styled-components";

export const ServicesWrapper = styled.div`
  width: 100vw;
  max-width: 100%;
  height: 100vh;
  background: rgb(23, 23, 23);
  font-size: 3vmax;
  display: flex;
  position: absolute;
  left: 0;
  top: calc(35% + 10vmin);
  margin: 0 auto;
  white-space: nowrap;
  overflow: hidden;
`;
